# N03 独立审核退回

最终状态：以下八组问题均已由 ZCode 修复，Codex 独立复验通过；全量 865 passed、14 skipped。本文保留问题复现与退回历史，最终结论见 [验收记录](AGENT_EVOLUTION_ACCEPTANCE_2026-09-06.md)。

第一轮 11 项通过，扩展后 6 failed / 11 passed；日志 `/tmp/codex-evolution-n03-boundaries-20260906.log`。独立脚本 `audit_evolution_comparison_2026_09_06.py` 现 17 例，由 Codex 维护。

实际 Web 最初另新增 `audit_evolution_web_2026_09_06.py` 两例，通过真实 Streamlit AppTest 点击和改变选择；初版两例均失败，分别复现入口缺失与旧下载残留，日志 `/tmp/codex-evolution-n03-web-audit-20260906.log`。后来增加非法 UTF-8 历史和分析运行中入口两例，最终本批独立共 21 例（核心/CLI 17 + Web 4）。

1. **P1 CLI 覆盖原报告**：`--output LEFT.json` 把源 JSON 覆盖成 Markdown。输出不得与任一输入指向同一文件（规范化路径、符号链接与已有硬链接均需检查）。报错时原报告保持逐字节不变；输出写入失败应清楚报错，不要留下成功提示。
2. **P2 下载丢失长报告末尾差异**：分析师截 80 行、交易员截 80 行、PM 截 120 行；长报告里的末尾变化不在下载内容里，后两种甚至不提示截断。完整 Markdown 下载必须保留所有差异。页面可折叠长差异，但不能把截断结果当完整文件。
3. **P2 旧指数报告被伪造为 stock**：两份无 metadata 的 `000001.SH` 报告被默认标为 stock。用明确类型或本地指数注册表可靠判定；无法确定时写未知，不能凭空默认。已知类型冲突仍拒绝。
4. **P2 unknown 被当可确认**：`data_quality.status=unknown` 时质量差异仍 comparable=true；旧报告不知道启用集合，却标为“已启用但空白”。未知质量不能作已知状态比较，未知团队的角色状态必须明确未知，不能推断已启用或未运行。
5. **P2 网页入口只在空闲首页**：真实浏览器点击历史报告后，页面没有对比入口，也没有回首页操作。请把对比入口放在所有非运行状态可到达的位置（侧栏合适），从单份历史报告进入并返回该报告。两侧默认选不同版本；历史行标识以稳定路径/run_id 为准，避免列表变动后索引误指。
6. **P2 旧对比结果与新选择可能错配**：当前只有 `cmp_result_md`，没有生成它的报告身份；用户改选择后旧结果和下载仍可见。相同两侧的错误分支也没清除旧结果。结果必须绑定生成时的两份身份；选择改变、同一份、历史列表变动或加载错误时清空/明确禁用旧结果与下载。侧栏点击单份历史记录要能退出对比。

7. **P2 非法编码拖垮历史列表**：畸形 JSON 已跳过，但非法 UTF-8 未捕获；一份坏文件会让有效历史也无法显示。历史与未完成记录索引均补齐编码错误处理。
8. **P2 后台分析时历史页仍可点击对比**：对比状态区已有非运行条件，历史页按钮却漏掉；点击后无效并留下对比状态。历史页入口应在后台分析时隐藏或禁用。新增 AppTest 使用实际 ProgressTracker 运行态复现，不启动真实分析。

以上均要求按最终用户流程修复，保留 N01/N02 和此前全部断言，不执行真实模型或行情。最终通过情况以 [验收记录](AGENT_EVOLUTION_ACCEPTANCE_2026-09-06.md) 为准。实际浏览器已核验单份历史进入对比、默认不同版本、下载触发、改选清除旧结果、旧新格式对比及返回原报告。

文案收口：对比正文去掉“canonical 优先、只比较一次”等实现说明，用户只需理解两份交易员计划的变化。完整性检查与事实准确率的区别保留。
