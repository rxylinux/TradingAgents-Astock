# F1 独立审核

## 最终独立验收（2026-09-09）：F1范围通过

R1/R2所有18项独立审计通过，与ZCode58项合并 **76 passed in 8.20s**。独立实际CLI正常检索selected=5/excluded=10，闭区间重叠仅rr-ov-after入选、excluded=2；两场景exit0，JSON/Markdown均包含输入文件摘要及所有选择/排除record_id。非法schema和NaN扩展字段真实CLI已由独立审计验证exit2且无成功报告。

独立全量 **1432 passed, 14 skipped, 52 subtests passed, 5 existing warnings in 73.52s**（exit0）；仓库ruff与diff检查通过。308文件测试前后比较，业务/测试全部不变，仅F2设计文档更新。快照与CLI产物：`/var/folders/1k/6mfcfpgd38bgh4s31fz6wsv00000gn/T/codex-f1-final-eni94901/`。

```text
2ebec79242290a1d594f1f308c1403a1096fa82c96fa3b2830e66a88c84d66f3 tradingagents/evaluation/review_record.py
717727c9e6f59cfd6e33c71cff2ee60ee6c84c626036d59131bc754bf49bc112 tests/test_review_record.py
52a37fde401d7d0d7d65c03e2d4cde8711c95f04d11d6f30e570bb5aafbf7226 docs/audit_f1_review_2026_09_09.py
7b2ce64762164ec89e54febff5e5b08cfce9c25ac4fc915935d9ab60dddeba18 tradingagents/agents/utils/memory.py
```

通过范围是F1离线复盘记录、五门AND时点检索、身份与摘要验证和CLI。生产记忆源码未变，未执行真实数据/模型效果实验；合成数据证明工程契约，不证明预测改善。F1相关性排序允许跨标的历史记录，F2按独立契约先过滤同标的同类型再限量。批准立即按[F2 Codex实施契约](F2_CODEX_IMPLEMENTATION_CONTRACT_2026-09-09.md)实施只读生产投影；F3设计另审。以下历史R1/R2保留。

## R1（2026-09-09 02:00）：暂不验收

独立新增 `docs/audit_f1_review_2026_09_09.py` 12项，**11失败/1通过**；与ZCode31项合并 **11 failed / 32 passed in 6.73s**（exit1）。通过控制例：合法记录能检索、null费用保留、修改输出不污染输入或下一次结果。未改业务实现或ZCode测试；不跑全量。

```text
.venv/bin/python -m pytest -q docs/audit_f1_review_2026_09_09.py tests/test_review_record.py --tb=short
```

### 已复现缺口

1. **P1 加载校验缺失（4例）**：schema_version=999、identity.run_id空串、identity.window为list、retrieval_meta.ranking_version=999均被接受。schema_version根本未比较；window仅在dict时校验、坏类型绕过。所有身份与版本用显式非bool类型/已知版本/非空字段校验，缺失、unknown与损坏分开，不靠取key不报错声称加载已验证。record_id/ticker/run_id/window/version/annotations等相邻形状一并完整校验，CLI报受控错误而非裸TypeError。
2. **P1 非有限费用（2例）**：fees_assumption.bps的NaN和Infinity通过（仅isinstance，没有finite）。统一复用有限非bool数值校验；费用单位及来源声明闭合，未知null不补零。规范JSON不允许NaN/Infinity，别用default=str掩盖非法对象。收益目前只有裸数值，需声明固定ratio:fraction规范与单位，拒绝冲突单位，不能靠数值量级推断百分数口径。
3. **P1 未来标注泄漏（1例）**：record_available_at=2025-05-07，但annotations.annotated_at=2099-01-01，完整未来correct_risk_flags仍进入2025-06-01历史检索JSON。五门覆盖原record不等于覆盖后添标签。可以拒绝版本可用时间早于标注的矛盾输入，或保留该版本排除并给明确原因；不能把未来标注原文放入历史投影。同类error_type/source/标注时间全部遵守版本与可知性约束，未到期不能借标注提前判定真实错误。
4. **P1 已验证外部快照未验摘要（1例）**：decision.thesis_digest含verification=verified_snapshot、非空snapshot和错误digest，全量load仍通过。当前只是检查snapshot存在，未复算。定义确定性快照digest并真正比较；有快照就验证失配拒绝，无快照必须明确external_unverified。evidence/config同类字段一致，不能由输入自称verified就升级。
5. **P1 去重破坏自身摘要（1例）**：加载两条相同record时，计算record_digest后往第一条追加_load_notes；输出记录已经与自身digest不符，重新load会失败。同一加载输出必须自洽、可再次验证；建议去重说明放结果外层元数据或明确规范化不改变记录语义，不能事后污染被摘要覆盖的记录。反序输入也不能把重复说明随机挂到不同record造成输出漂移。
6. **P2 排除清单不稳定（1例）**：同两条未来输入反转顺序，最终excluded列表顺序也反转；违反契约反序输入输出稳定要求。结果及排除理由按稳定身份/原因排序，limit元数据也确定；不只检查入选records排序。
7. **P1 真实CLI错误成功（1例）**：schema999文件实际exit0并生成JSON/Markdown（selected=1），不是仅纯函数校验问题。所有契约拒绝必须exit2，stderr清单且不生成成功报告。控制CLI需保留合法记录可正常演示。

### 同一契约的相邻完成项（本轮未计为失败）

- `retrieve_as_of`目前直接消费任意列表，不重验schema/digest，也没有显式不可伪造的已验证输入类型；load输出是可变dict。纯API应在入口验证，或定义可复核已验证快照，避免调用方修改record后绕过摘要/状态校验。不把任意非pending状态当resolved。
- F1权威规则7是同标的优先、同类型次之排序；当前却先排除所有异标的/异类型，后两排序项永远无效。请按F1契约恢复排序语义，F2接入则另行明确“仅相同标的和类型”过滤，不混淆两层要求。F1原测试如果只覆盖同标的排序不能证明完整排名。
- 已声明事件有效期与标注来源/时间要实际消费；没有字段保持unknown/无约束，不伪造过期。bool不得冒充schema/ranking版本1。
- CLI Markdown目前排除仅原因计数，不列record_id逐条对应；input_file_digest也只在JSON，没有规则要求的输入摘要/可审计排除清单完整呈现。两出口应保留核心参数、输入digest、单位、限制与逐条选择/排除身份；不能仅因JSON有字段就声称Markdown全量。
- MAX_RECORDS只在CLI加载/去重后检查，纯load无上限、重复记录可在去重前超额处理。统一约束原始条数与文本字节，limit给实际有限上限，拒绝非法对象，避免只把CLI末端检查当入口保护。

优先修F1 R1及同类边界，定向交接。F2文档可继续，F1通过前不进入F2生产接入。不编辑Codex审计。

本轮SHA-256：

```text
85daf1065dd7c3c82b2613051d64ea9aa30f2ed09ae9edb706b87752f2aefa58 tradingagents/evaluation/review_record.py
16589c308339dbfdc4fa462223c1ef6e978382f57a6c89e82d521b4da0b46f4f tests/test_review_record.py
0ce7b30d94cfc4c4db88559db03821d657582bbe917ddc67a401a9bfacc1a2ee docs/audit_f1_review_2026_09_09.py
```

## R2（2026-09-09 02:14）：12旧项通过，6项新增失败

R1独立12项与ZCode47项合并 **59 passed in 6.54s**。相邻边界新增6项后，独立审计 **6 failed / 12 passed in 3.33s**（exit1）。不跑全量。

1. **版本类型仍不严格（2例）**：schema_version=True被当1；retrieve_as_of(ranking_version=True)也被当1。此前仅record.retrieval_meta内拒bool，另外两入口漏掉。统一已知正整数版本且非bool的校验，不用Python宽松等号冒充类型正确；浮点1.0同类应拒。
2. **跨标的被重叠过滤（1例）**：修复F1恢复相关性排序后，overlap条件仍对所有记录生效。查询600519，记录000001窗口重叠却被排除；契约#8明示仅相同instrument/type的query-window排除。异标的仍可按F1排名作为经验，不在此套窗口排除；F2严格同标的过滤另做。
3. **快照校验依赖自述标签（2例）**：同一错误snapshot+digest，只要verification缺失/None或external_unverified，就跳过复算。契约#9是“提供明确快照则复算并拒绝失配”，不由调用方标签决定是否检查。有snapshot时始终验证；无snapshot时统一明确external_unverified，不仅默许缺标签。语义解释和原始digest不能靠任意声称verified升级。
4. **真实CLI非有限JSON非受控拒绝（1例）**：扩展字段extra_annotation=NaN通过json.loads，后来canonical digest抛ValueError，实际exit1+Traceback；应在JSON加载/递归合法性校验阶段形成RecordValidationError，exit2无成功报告。全部字段的NaN/Inf、非法类型、不能UTF-8编码的字符串等都应进入可解释错误边界，而非依赖部分业务字段检查。

请修完上述同类边界并定向交接，保持独立审计原样。F2权威契约由Codex另文件定稿，F1通过后立即启动，不在当前复验期间改F1业务源码之外的已验收模块。

R2 SHA-256：

```text
6952c889dc797546caec392606b2d26ab4f46fd8e737916236efe4f144935200 tradingagents/evaluation/review_record.py
52a37fde401d7d0d7d65c03e2d4cde8711c95f04d11d6f30e570bb5aafbf7226 docs/audit_f1_review_2026_09_09.py
```
