# F2 独立审核

## 最终验收（2026-09-09）：F2 按已验证范围通过

- 独立F2 26项+ZCode53项：**79 passed in 1.86s**，覆盖R1/R2/R3失败复现、真实prepare默认stock/index提示词零差异、启用五工厂、SQLite恢复及报告损坏控制。
- 独立完整命令`.venv/bin/python -m pytest -q`：**1511 passed / 14 skipped / 52 subtests passed / 5 existing warnings in 89.07s**，exit0。跳过来自未安装的可选SDK/Gemini；不称这些路径已运行验证。
- `.venv/bin/ruff check .`与`git diff --check`通过。全量前后314文件快照比对，仅ZCode F2交接文档有变化，全部业务/测试哈希一致。HEAD仍d269ba4，未提交/推送。
- 接受内容：显式只读复盘文件在fresh准备期按同标的/类型及五门时间投影，默认关闭零提示词差异；运行元数据锚、摘要复算、真实恢复纯读、五工厂共用入口及JSON/MD/PDF/Web展示。用户历史记忆未改；生产memory.py哈希保持一致。
- **范围边界**：实际生产上下文为字符串。提示词调用方仍用`str(value or "")`，None变空串并被拒绝，数字等类型可能被规范成字符串；strict辅助API显式None参数目前按未提供处理。故不声称该辅助API对任意调用者输入提供全面原始类型校验。此边界与已验证的生产身份比对区分。结构自洽不证明外部记录是真实历史，来源声明不升级为事实；合成绿测不证明预测效果提高。

后续按独立F3 Codex实施契约开始F3a→F3b。F3不改生产决策路径，全部批准批次验收后收口。

最终SHA-256：
```text
4cefc75cc2df569ab85e575a9df36e5712bb962ba3a37a2aa32d7c441d6c90b4 tradingagents/evaluation/review_projection.py
2240fb52d6afaa552229408a0525d0dae62d558f1d0fc115cfbb11fb8a79ef70 tradingagents/graph/trading_graph.py
2c161a3753ffce60de9e1f37de24ad25d85e3bc00721bb5d43dcc8f5f27b49a8 tests/test_review_projection_integration.py
029563d15258f3e4b7fff1d53bcaefc21f0a26055428c813357ee0363e806c99 docs/audit_f2_projection_2026_09_09.py
7b2ce64762164ec89e54febff5e5b08cfce9c25ac4fc915935d9ab60dddeba18 tradingagents/agents/utils/memory.py
```

## R3（2026-09-09）：真实默认路径回归，仍暂不验收

R2原22项独立+ZCode45项 **67 passed in 1.91s**。新增4项真实路径/相邻控制后 **4 failed / 22 passed in 1.58s**（exit1），审计ruff及git diff --check通过。未跑全量。

1. **真实fresh默认关闭污染提示词（stock/index共2例）**：使用现有测试构造壳调用真正TradingAgentsGraph.prepare_graph_run，得到fresh持久enabled=False锚、无payload；分别调用真实Research Manager工厂，对比移除F2锚的原提示词，新增invalid-projection“投影不是对象”段。两次均只调用1次模型，差异来自F2自身。R2调整mode判断后，明确disabled分支没有返回空块而继续验证None。必须在合法关闭态零块，并在真实prepare→五工厂路径验证stock/index默认零差异；不要仅拿空字典证明默认兼容。损坏enabled态仍拒绝。
2. **报告损坏对象仍裸崩（2例）**：run_binding或trusted_context为list，重算payload_digest，render_projection_md分别在validator和schema时间分支.get触发AttributeError。R2交接称已受控，但未覆盖此实际入口。schema检查报告错误后仍继续使用坏对象，外层又靠错误文本过滤“可信/trusted_context”冒充结构模式，不能视为完整分层。请显式结构-only/严格归属两种校验模式，结构检查失败后不解引用坏形状，不按文案删除错误；报告及提示词对坏输入都受控，不回显历史正文。归属模式必需字段仍强制。

顺带统一检查：as_of不能解析时不可跳过全部时间门；当前调用者的str(...)不应把错误类型转成“合法可信身份”。这些列作完成边界，不额外计入已复现4失败。先让真实默认控制、正常启用、缺字段/错时间、SQLite恢复控制一起通过，再交稳定哈希。Codex独立脚本保持不动，F3设计调和可继续，F3业务实现等F2验收。

```text
.venv/bin/python -m pytest -q docs/audit_f2_projection_2026_09_09.py tests/test_review_projection_integration.py --tb=short
.venv/bin/python -m pytest -q docs/audit_f2_projection_2026_09_09.py --tb=short
fa6937f0d6f836aa420addb23cbf3417f15cf635766ecf4f970974375a2ebe4c tradingagents/evaluation/review_projection.py
2240fb52d6afaa552229408a0525d0dae62d558f1d0fc115cfbb11fb8a79ef70 tradingagents/graph/trading_graph.py
029563d15258f3e4b7fff1d53bcaefc21f0a26055428c813357ee0363e806c99 docs/audit_f2_projection_2026_09_09.py
```

## R2（2026-09-09）：仍暂不验收

R1原14项独立与ZCode35项：**49 passed in 1.85s**。新增8项相邻契约探针后，独立脚本 **8 failed / 14 passed in 1.66s**（exit1）。审计ruff与git diff --check通过；未跑全量，未改业务/ZCode测试。

1. **缺少可信上下文仍放行（3例）**：直接提示词入口删除company_of_interest、instrument_type或trade_date，仍给模型原rating与收益。统一校验器的`if trusted`仍跳过缺失；必须区分“仅结构验证供报告”和“强制可信上下文验证供准备/恢复/工厂”，后者任何必需身份缺失都拒绝，不能把空字符串当可选模式。三字段必须非空且正确类型，run_id同理，不应str(None)制造身份。
2. **有enabled锚却缺payload被伪装为关闭（1例）**：projection_for_prompt在检查元数据前return空串。先分辨mode与结构，再返回默认空块；此情形给受控invalid说明。真正未启用/旧缺席控制保留。
3. **快照时间没有schema与可见性校验（2例）**：maturity_date改2099或decided_at改not-a-time，重算payload与锚后仍渲染值。本例验证嵌套schema/时间契约，不是绕过摘要鉴权。沿F1严格上海时间原语验证字段、AND可见性；保留足以复核五门的必要观察/发布/版本可用时间，不能只留两个日期却宣称复核全部合规。来源自述与已验证不混同；保持fresh正常、null与合法时间控制。
4. **锚版本接受float（1例）**：schema_version=1.0被等号当1；准备/恢复/提示词共同要求已知int且非bool，不能只检查bool。
5. **MD/PDF/Web公共渲染丢可复核信息（1例）**：真实六条输入只选五条，payload.limit_truncated=1，但render_projection_md没有截断说明；也未声明overlap_filter=not_requested。JSON与三展示出口均需保留限制和截断信息；提示词总预算仍包含标题与尾注。

同类检查：提示词错误说明不要原样插入未经验证的历史正文/巨大字段；报告结构验证面对list run_binding/context等应受控而非裸异常。新版必需时间字段与旧F2草稿快照兼容策略明确，不能把缺字段默认“可见”。真实五工厂共享修正路径，恢复用同一校验器；原SQLite/past_context控制继续通过。

```text
.venv/bin/python -m pytest -q docs/audit_f2_projection_2026_09_09.py tests/test_review_projection_integration.py --tb=short
.venv/bin/python -m pytest -q docs/audit_f2_projection_2026_09_09.py --tb=short
888c55f81b5c80c3956b7eaaf207c78b1610c7a2cd419cba89b74947e63bd7e4 tradingagents/evaluation/review_projection.py
2240fb52d6afaa552229408a0525d0dae62d558f1d0fc115cfbb11fb8a79ef70 tradingagents/graph/trading_graph.py
cdb89b435bef9c2bc4b7b7c93beb1006881c56295bc0803d4dc13ac079f4b51b docs/audit_f2_projection_2026_09_09.py
```

## R1（2026-09-09）：暂不验收

新增独立 `docs/audit_f2_projection_2026_09_09.py` 14项，13失败/1控制通过；与ZCode23项合并 **13 failed / 24 passed in 1.97s**（exit1）。合法投影/默认缺席控制通过。未改业务或ZCode测试，不跑全量。

```text
.venv/bin/python -m pytest -q docs/audit_f2_projection_2026_09_09.py tests/test_review_projection_integration.py --tb=short
```

### 失败分组

1. **提示词缺独立元数据锚验证（4例）**：删除run_metadata.review_projection、改其input_digest、改其payload_digest、删除可信run_id，projection_for_prompt仍输出原rating与0.12。payload自身摘要自洽不等于属于本run。共用完整state验证器需核对独立锚、非空可信身份、上下文、schema/ranking/policy/模式与两个摘要；不得“trusted非空才比较”，缺可信值本身即无效。
2. **实际RM工厂泄漏（1例）**：真实create_research_manager+捕获LLM（仅1次调用）在元数据payload_digest不符时，仍把AUDIT_F2_VALUE/收益数值交给模型。不是纯helper边缘；五个工厂都走同一完整校验，无效只给受控invalid-projection、无历史正文/数值。保留合法控制与调用次数。
3. **损坏输入变异常而非受控说明（2例）**：trusted_context为list触发AttributeError；selected收益NaN在摘要复算时抛ValueError。必须先做完整递归shape/JSON合法性检查，摘要计算错误转换为明确校验问题。直接提示词入口不可裸崩溃，准备/恢复则抛受控错误且保留断点。
4. **恢复完整性与模式遗漏（5例）**：运行锚enabled=True但payload删除；可信run_id删除；锚schema999；锚ticker改000001；配置关闭而断点含启用投影，均被_validate_resumed_review_projection放行。不能在payload缺失时先return；先检查持久mode/schema锚和当前模式，再处理payload有无。fresh关闭也持久模式锚；双向切换按契约拒绝。真正旧无投影在关闭模式兼容，不能用恢复时才配置文件补投影；存在锚但缺payload不是旧态。锚所有声明字段须比对，不能只挑两个摘要。
5. **摘要自洽的非法快照被接受（1例）**：selected.raw_return改为字符串not-a-number并更新自身与元数据digest，helper仍渲染它。此probe检验schema校验，与摘要篡改检测分开；完整schema必须包括selected数量/记录结构/有限非bool值/已知单位/时间/来源状态，统计非负整数、版本非bool整数、limitations字符串等。哈希不替代字段验证。

### 同类完成要求（避免逐字段补洞）

- 统一有可信state/context的校验层供准备/恢复/五工厂调用；渲染自身验证只能称结构自洽，不能在没有state的情况下称run归属已证明。检查锚input_digest缺失、projection缺失/None/坏类型、trusted_context与metadata坏类型、bool版本、空run与错type/date等对称情况。
- 真正SQLite关闭重开+prepare+invoke(None)同时验证同模式正常、两种模式切换、篡改断点仍保留以及零模型/工具调用；不能仅调用validator就声称生产恢复已覆盖。
- 现有past_context刷新控制只在字典里直接赋值，尚不能证明_safe_resume_checkpoint真实刷新不覆盖投影。请增加真实更新路径控制（上下文替身必须一致可解释，勿用None/空串差异制造假失败）。
- 输入摘要宣称覆盖原始文件字节时保留newline/UTF-8原字节；快照必要时间/来源信息应足以解释F1五门与版本可知性，不能只留收益、到期日而丢全部观察/发布/版本来源说明。未来排除正文仍不进模型。
- 统一JSON/MD/PDF需保留limit_truncated和overlap_filter=not_requested及验证边界；声明的1200/160上限应包含所有头尾文字。CLI/Web入口要实际prepare到工厂，不只测参数定义存在。

先修F2 R1与同类边界，定向交接；F3仅文档，不能绕过已知失败。Codex审计不改动。

开发快照 SHA-256：

```text
b19ef2c8cc85c811edae0ba6e21f65f86d123042911a1072c6f780a47a3f70ce tradingagents/evaluation/review_projection.py
c36fb09a8ed16e69511d5c469b389ca441c023b2369d4082fd6a5fbd5488e000 tradingagents/graph/trading_graph.py
dad0c51aeabedf7093c10c749861f42a22e056dc4afd5cd3186d79fcf24994db docs/audit_f2_projection_2026_09_09.py
```
