# 给 ZCode 的下一轮启动指令

工作目录：`/Volumes/solid hard disk/github/rxylinux/TradingAgents-AStock`。

用户已要求“做一个计划让zcode写代码”。请延续分工：你负责业务实现与自身测试，Codex 负责方案、独立审核与验收。上一轮 A→F 已结束，本次是新的有限队列。

请先完整读取：

1. `docs/NEXT_PHASE_IMPLEMENTATION_PLAN_2026-09-10.md`
2. `docs/G1_FINANCIAL_SNAPSHOT_CONTRACT_2026-09-10.md`
3. `docs/USER_GUIDE_DECISION_QUALITY_2026-09-09.md`

立即从 **G1 财报原始快照** 开始编码，不仅回复计划。先记录当前基线与相关源码/测试摘要，保护上一轮尚未提交的所有代码。复用现有 Sina 直连和 requests，不引入 akshare，不修改 D1 公式，不从过滤后的 DataFrame 反造原始响应。做好快照身份、失败/空数据区别、有限请求预算、原子写入和离线读回。

按照 G1 契约编写有意义的失败测试并实现 CLI；开发中定向回归，稳定后写 `docs/ZCODE_G1_HANDOFF_2026-09-10.md`，包含实际命令/退出码、文件 SHA-256、可复验样例、真实/合成数据标识和遗留限制。不能改 Codex 审计脚本刷绿，不能自行宣布已通过独立验收。

G1 交接后可先整理 G2 来源字段映射表；后续按总计划 G2→H1→H2→I1→I2 推进，进入每批前以具体契约为准。共享文件冻结审核时做不冲突的设计/样例准备，收到 Codex 失败反馈先修复。

默认不提交/推送、不调用付费模型、不真实交易、不改用户历史缓存/记忆/全局环境。测试写临时目录，网络策略阻止实采就标“线上未验证”，不能绕过权限或把 synthetic 标为 captured。不要新建常驻调度或恢复旧心跳。

当前交接状态：本文件已写入仓库；Codex 的 Computer Use 未获准操作 ZCode，因此**尚未自动发送、未确认开工**。当你实际收到此启动指令后，请在交接文档写明接收与开工状态，不把本段静态文本当作已经启动的证据。
