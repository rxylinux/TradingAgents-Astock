# N04 独立审核记录

状态：**全部阻断问题已修复并独立验收通过**，最终 1009 passed、14 skipped、52 subtests passed、5 warnings，详见 [最终验收记录](N04_ACCEPTANCE_2026-09-06.md)。下文保留各轮发现时的事实、复现和修复要求，属于审核历史；不代表当前仍有同样失败。

原有基线 865 项；Codex 独立维护 `audit_n04_core_2026_09_06.py`、`audit_n04_workflows_2026_09_06.py`，最终新增 108 项独立审计，ZCode 新增 36 项回归。首次语法未收口的导入失败不计业务缺陷。可导入版本首轮实测 48 passed / 16 failed，日志 `/tmp/codex-n04-independent-r2.log`。

## R1：交付前必须修复

1. **P1 汇总的声明分母错误**：对 trial 比率取平均，而非汇总声明。1 条 supported 与另一个 trial 的 9 条 unverifiable 被算 50%，应为 10%；时点违规同理。汇总 supported/contradicted/unverifiable/future_evidence，并按总声明/有引用声明数分别计算。独立 `test_fact_summary_uses_claim_denominators_not_mean_of_trial_rates` 复现。
2. **P1 已失败 trial 的新增退化被吞**：比较仅在 baseline 整体通过时扫描检查。原本 rating 已失败，再丢 run_metadata 却没有退化明细，CLI --fail-on-regression 返回 0。逐项扫描所有配对 trial 的 pass→fail/unknown/absent，并在 CLI 中纳入门控。可保留整体四分类，但不能以整体分类替代检查退化。
3. **P1 任意 JSON 被当成合法评测比较**：`compare_evaluations({}, {})` 返回 comparable=true；重复 trial 被 dict 覆盖；计数、contract_pass 类型、固定八项 checks 缺失均不校验。入口要验证版本/digest/身份唯一、trial 状态和固定检查集合、契约布尔与检查一致、metrics 分母/计数/比率一致；无效文件 CLI 返回 2 不写结果。详见七组 compare 导入测试。
4. **P2 空标注被当 0%**：配对用 `(rate or 0)` 把空 claims 的 null 与有值比较，误称支持率改善。只有双方该指标均非 null 的配对才统计，另外给出该指标共同覆盖数。没有共同覆盖就不判断变化。
5. **P1 坏报告中止全套评测**：核心只验顶层 dict，market_report=dict 抛 AttributeError；final_decision=int 仍算 completed。CLI loader 把 JSON/UTF-8 错误包成 ValueError，evaluate 不捕获又导致 traceback。校验已知报告字段（含元数据/质量嵌套）的类型；不可评分报告按单条 invalid 继续，保留控制异常传播，不回显完整正文/私有配置。
6. **P1 路径边界失效且错误阶段不对**：用字符串 startswith 会把 sibling `input-secrets` 当作 `input` 子目录；`inside/../report.json` 被接受；absolute 只在 loader 内错误然后生成 successful evaluation，不按路径格式错误拒绝整份清单。执行前检查所有路径格式/真实包含关系（Path.relative_to/is_relative_to，不做字符串前缀）；符号链接逃出也拒绝。单纯缺失/不可读/坏 JSON 仍是该 trial invalid，须与路径格式违规分开。
7. **P2 旧记录被补成已知、损坏档案可通过**：缺类型的普通股票被默认推断 stock（应 unknown），saved quality=unknown 被改成 fail（应 unknown）；run_metadata 缺 created_at/type 仍通过；质量 analysts 额外/重复角色被字典吞掉。新增核心边界审计覆盖这些情况，元数据须符合 N02 完整结构再核对指纹与案例。
8. **P2 schema 错误不一致**：bool schema_version 冒充 1；run.case_id=[] 抛 TypeError，suite_digest=null 在错误消息切片时崩溃。类型先校验再 set/切片，统一 EvaluationInputError；同类枚举/列表/哈希字段一并收口。

代码阅读补充：公开 `grade_report(..., review=...)` 目前忽略 review 参数，请与 evaluate_suite 统一标注处理结果；`datetime.fromisoformat('...Z')` 在项目声明支持的 Python 3.10 不可用，应集中解析时正规化 Z 为 +00:00，保留时区/微秒；不能依赖当前 3.13 测试环境掩盖兼容性。

写出失败清理也需核对：创建输出目录竞争失败时不能 rmdir 别人已创建的目录；文件写到一半抛错时需记录并清理本次文件，不能因清理失败掩盖原错误或删除非本次文件。最后按任务书继续完成静态案例、README/用法与交接，暂不跑全量，等 Codex 独立复验。

上述公开 review 接口和两种输出失败已补实际注入测试，独立共 77 例。示例与使用说明还需收口：当前用“合成证据B”支持“Rating: Buy”，这不是可核验事实的示范。请将演示改为具体合成事实（如“演示营业收入为 42 单位”），让报告 quote 与证据正文确实表达同一事实，再更新固定 digest；评级/建议属于判断，不要在示例中作为 supported 事实。合成性质必须明确。

报告文案：`evidence_backed_claims` 仅表示“引用了证据的声明数”，包含 contradicted 和未来证据；不能渲染为“有证据支持的声明”，应与真正 supported 数分开展示。未知值可显示“未记录/无法计算”，避免给普通使用者只展示代码中的 null；JSON 保持 null。

## R2：首轮修复后的边界收口

首轮 77 项已全部复验通过；扩展后 77 passed / 11 failed，日志 `/tmp/codex-n04-independent-r4.log`。另补两项写出所有权/缺失 trial 明细，目前独立冻结为 90 项。

1. 嵌套类型仍未校验：顶层或质量卡 selected_analysts=[{}]、analysts.role=[]、metadata.config_snapshot=list 会中止套件。补齐嵌套字段的类型检查；将 digest/grade 期间确定的报告输入错误也纳入单条 invalid 边界，控制异常继续传播。created_at='not a date' 不能算有效档案；损坏 run_metadata 与缺失档案区别标为 fail/unknown。
2. compare 的 schema 收紧误伤正常产物：failed/missing/invalid trial 依法没有八项评分，当前一律要求八项而拒绝自己的合法产物。completed 才须完整且唯一的八项；其他状态须空检查、contract_pass=false。新增同一份含四种状态产物自比较测试。
3. compare 导入语义仍可作假：checks 内已有 fail 但 contract_pass=true、重复 check、metrics.fake rate、metrics 状态计数与真实 trial 相反、导入 observations=NaN 均被放行。按 trial 重算检查布尔、各状态计数、provided、三项固定分母率，并复用 observations/review 结构校验；别只检查总数之和。
4. missing candidate 的原 pass 检查目前被 `if ... is None: continue` 跳过。按 absent 列出丢失检查及 case/trial，Markdown 也必须看出缺的是谁。扫描仅 pass→fail/unknown/absent，not_applicable 不当作额外退化。
5. 写出失败清理用 rmtree 删除整个本次目录，仍会删除期间别的写者放入的文件。禁止递归删除；登记本次实际新建的文件，含写到一半的当前文件，只清理这些文件，最后 rmdir 尝试删除空目录即可；若有非本次文件保留目录，仍返回原错误。新增真实 tmp_path 中第二写者文件保留测试。

请同时完成上方的示例证据/中文文案收口与 README 入口，不要仅以审计全绿替代完整任务书。原 865 项与旧业务代码仍不动，最终全量由 Codex 执行。

R2 代码复核补充：最新比较器仍接受 provided=999、非空套件 contract_pass_rate=null、负费用、review=[]、bool schema_version。这些已用真实生成产物修改后重放确认，补为同组 5 项，独立共 95 项。不要为每条断言加零散特判：校验所有 consumed 字段的完整类型/枚举，复用 observations 校验，按 trial 重算 completed/failed/invalid/missing/provided、未知检查数及分母率；schema 整数拒绝 bool。建议将汇总口径归为一个共享函数，供 evaluate 和 compare 导入校验复用，减少平行算式漂移；有效零值要保留，缺失比率不得冒充合法非空套件输出。review 已处理结果需匹配 reviewed/invalid/unreviewed 的结构与计数/比率，空列表不是无标注。

## R3：最终语义复核

95 独立 + 28 ZCode 定向结果目前 122 passed/1 failed（`/tmp/codex-n04-targeted-final.log`）：null_rate 仍未拒绝。校验注释写了“非空分母不可 null”，但实际只校验 `if rate_val is not None`，还缺反分支。

另有三个实际反例，已补独立断言（现独立 98 项）：

- **P1 人工事实比率仍可与声明矛盾**：一条 unverifiable、supported=0 的合法已处理 review，只把 factual_support_rate 改为 1，compare 就输出 support_improved=1。仅检查区间 0–1 不够，必须从 processed claims 的 verdict/evidence_ids/has_future_evidence 重算声明计数和两种比率；或共享评分端的声明汇总函数。缺失/invalid/unreviewed 的比率应为 null，reviewed 空列表也为 null。不要改变分母规则来刷绿。
- N02 `config_snapshot=null` 被 `or {}` 修成空快照；搭配空字典 digest 后整个契约通过。null 不是合法 dict 档案，标 fail/invalid，不做隐式修复。
- N02 `schema_version=true` 被等同于 1，整个契约通过。结构化 schema 需确为整数 1；损坏记录和完全缺失区别处理。

以上修复后只做定向与示例真实 CLI，交接计数如实，之后由 Codex 全量收口。

### R3 同类遗漏：必须统一汇总实现

98 独立 + 28 回归已实测 126 passed。但当前实现只比较两个非 null 比率，没有核对重算计数，也没有验证全局人工事实/观测汇总。新加 10 个同类反例（独立共 108 项），日志 `/tmp/codex-n04-r3-consistency.log`：非空 review 的比率改 null、supported 计数改 0、reviewed.claims=null、非法 verdict、无证据却给时点率、顶层事实率/费用/unknown_checks 被改动以及 completed.review=null。它们都应按非法产物拒绝，不能绕过或崩溃。

请完成统一实现，别再按单个反例打补丁：

1. 从合法 processed claims 计算五种计数、声明数及两种比率，评分端和导入端共享；严格检查每个 claim 的字典/字段/合法 verdict/字符串 ID 列表/布尔时点标志，先验结构再计算。计数精确匹配；期望 null 就必须 null，期望数值则必须非 null（数值可用 isclose）。reviewed 必须 claims list；invalid/unreviewed 必须空声明和 null 计数/率；completed 的 review 必须为有效对象。不能将缺失明细当成已核验。
2. 将全部 metrics 汇总提取为由 trials + expected/provided 生成的共享函数，evaluate 和 compare 复用。校验完整汇总，包括所有四种状态、unknown_checks、人工覆盖/计数/比率、observations 各 observed_count/mean/currency，避免平行算式和默认值掩盖缺失。比较器不得直接信任并展示不一致的缓存统计。不要改本轮既定公式与 null 规则。
3. 补齐类型/枚举/固定检查集校验后再访问字段，非法产物统一 EvaluationInputError（CLI exit 2）。自己增加相邻分支的回归，不仅依赖我的 108 项。
4. README.md 目前仍没有 N04 入口；请添加离线评测的用途、命令入口和 examples/evaluation/README.md 链接。交接将 N03 REPORT_COMPARISON_GUIDE 误列为 N04 新交付、示例写 8 个 absent（实际上 7 个 pass，另一个 not_applicable）须更正。示例命令输出目录需唯一或明确必须不存在。

仍只修 N04 新包与本轮测试/文档，不改已验收旧业务。完成后定向、实际 baseline/candidate compare CLI，冻结供最终全量验收。

共享实现读码检查清单（请在当前交接前一起处理，属于上述同一要求）：

- `_process_review` 目前仍手写声明汇总，没有调用新 `summarize_review_claims`；须实际复用，不只加一个并行函数。
- 实测 processed claim 的 `has_future_evidence=null`、`evidence_ids=[1]`、`reviewed_claims_count=999` 仍被接受；check_id=[] 抛 TypeError；metrics.completed=true 当成 1。claim 必填字段/元素类型、布尔标志、完整计数应严格检查（含 field 与非空唯一 claim_id）；枚举值先验 str 再查 set，计数先验 int 且拒绝 bool。
- 有观测 latency=5、count=1，但汇总 mean=null 仍被接受；汇总 observations 顶层/币种项先验 dict，均值与共享汇总用同一 nullable numeric 比较，不能只写 `elif actual is not None`。空分母与非空分母两边都验证；统一 EvaluationInputError 而非 TypeError。
- 请为上述相邻分支自行补回归，独立 108 项不变。避免只跑我已有断言而漏读任务书和这份审查说明。

真实静态示例 CLI 复核：run/run/compare/self-compare/refuse-overwrite/digest 均得到预期退出码，但任务书要求的混币示例实际上不存在，baseline/candidate 的 observations 只有 USD（交接却写 USD+CNY）。请在静态 baseline 的 index_normal/trial1 加一组明确合成 CNY 成本，在 candidate 同一 trial 加明确合成 USD 成本，演示跨币不能配对；保留已有 USD 观测和缺失值。这不修改报告正文或 suite，不需改变它们的 digest。更新使用说明，实际核验两币分组且该配对没有 cost_comparison。所有示例数据仍必须写明合成、非实测。

最后一个 checklist 条目还未对应：ZCode `test_invented_reviewed_count_rejected` 实际修改 supported=999，而报告的问题是 **reviewed_claims_count=999**；当前该字段仍被忽略。请把这个回归改为真实改动 reviewed_claims_count，验证它若存在须为非 bool 整数且等于 len(claims)，invalid/unreviewed 若存在则须 null。既有空 review 的输出未记录该字段，可保留兼容缺失；其他五种计数仍按完整合同校验。其余核心类型修复已读码确认，不扩检查范围。
